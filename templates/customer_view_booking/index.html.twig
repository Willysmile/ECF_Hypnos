{% extends 'base.html.twig' %}

{% block title %}Hello CustomerViewBookingController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    Retrouvez-ici l’historique del’ensemble de vos réservations.

    Nous vous rappelons que la date limite pour annuler est de J-3 avant le début du séjour


{% for booking in bookings %}


    <div class="card">
        <div class="card-body">
            {{ booking.suite.hotel }}
            {{ booking.suite.name }}


            {{ booking.StartDate |date('d-m-Y')}}
            {{ booking.EndDate |date('d-m-Y')}}


            <form method="post" action="{{ path('app_booking_delete', {'id': booking.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booking.id) }}">

                {% if date(booking.StartDate) >= date('+3days') %}
                <button class="btn">Annulation de la réservation</button>
                {% else %}
                    <button class="btn disabled">Annulation de la réservation impossible</button>

                    {% endif %}
            </form>
        </div>
    </div>

    {% endfor %}
<div class="example-wrapper">
    <h1>Hello ! ✅</h1>


</div>
{% endblock %}
