{% extends 'base.html.twig' %}
{% block title %}Edit Suite{% endblock %}

{% block body %}
  <h1>Editer les suites</h1>

  {{ include('manager_suite/_form.html.twig', {'button_label': 'Update'}) }}

  {{ form_start(form) }}



  <h2>Images</h2>

  <div class="container d-flex">
    <div class="row ">

  {% for suite in suite.images %}
    <div class="col-md ">

      <div class="card my-2" style="width: 200px;">
        <img class="card-img-top" src="{{ asset('/uploads/'~suite.name) }}" alt="Image suite">
        <div class="card-body">





        </div>
      </div>
    </div>
  {% endfor %}
    </div>
    </div>

      </div>

  {{ form_end(form) }}

  <a href="{{ path('app_manager_suite_index') }}">Retour à la liste</a>

  {{ include('manager_suite/_delete_form.html.twig') }}
  {% block javascripts %}

  {% endblock %}
{% endblock %}
