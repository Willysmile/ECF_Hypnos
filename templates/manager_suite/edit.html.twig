{% extends 'base.html.twig' %}
{% block title %}Edit Suite{% endblock %}

{% block body %}
  <h1 class="my-5">Editer les suites</h1>

  {{ include('manager_suite/_form.html.twig', {'button_label': 'Valider les informations'}) }}
<div class="d-flex my-4">
  <a class ="btn btn-primary m-2" href="{{ path('app_manager_suite_index') }}">Retour à la liste des suites</a>

  {{ include('manager_suite/_delete_form.html.twig') }}</div>
  <hr>  <hr>

  <div class="container d-flex my-5">
    <div class="row ">
      {% if suite.images is not empty %}<h2>Images</h2>
      {% endif %}
  {% for suite in suite.images %}
    <div class="col-md ">
      <div class="card my-2" style="width: 200px;">
        <img class="card-img-top" src="{{ asset('/uploads/'~suite.name) }}" alt="Image suite">
        <div class="card-body text-center">

          {{ include('manager_suite/_delete_images.html.twig') }}

        </div>
      </div>
    </div>
  {% endfor %}
    </div>
    </div>








  {% block javascripts %}

  {% endblock %}
{% endblock %}
