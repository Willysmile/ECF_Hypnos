{{ form_start(form) }}
<div class="form-images">
<div class="my-custom-class-for-errors">
	{{ form_errors(form) }}
</div>
{{ form_row(form.name) }}
  {{ form_row(form.description) }}
  {{ form_row(form.night_price) }}
<button class="btn btn-primary m-2">{{ button_label|default('Valider les informations') }}</button>
<a class="btn btn-primary m-2" href="{{ path('app_manager_suite_index') }}">Retour à la liste des suites</a>

{{ include('manager_suite/_delete_form.html.twig') }}

<hr>
<hr>
<div class="vichr_resize_form">
	{{ form_row(form.imageFile) }}</div>

<div>

	<button class="btn btn-primary m-2">{{ button_label|default('Valider l\'image en avant') }}</button>

	{{ form_row(form.images) }}
	<button class="btn btn-primary m-2">{{ button_label|default('Valider les images de la suite') }}</button>
</div>
</div>

{{ form_end(form) }}