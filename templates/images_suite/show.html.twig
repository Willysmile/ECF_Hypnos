{% extends 'base.html.twig' %}

{% block title %}ImagesSuite{% endblock %}

{% block body %}
    <h1 class="h1">ImagesSuite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ images_suite.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ images_suite.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_images_suite_index') }}">back to list</a>

    <a href="{{ path('app_images_suite_edit', {'id': images_suite.id}) }}">edit</a>

    {{ include('images_suite/_delete_form.html.twig') }}
{% endblock %}
